# Systematics Post-Fix Audit (pre-analysis) — tab_systematics.tex regeneration

Date: 2026-06-09. Written BEFORE the script.

## 1. Current tab_systematics.tex content
Generated by the pre-fix `paperSystematics.C`: per-build σ(150) shifts under selection variations
(K=500/2000, r_fid=2.5/3.5, MCP<700, HG>30, fit-range row that is identically 0 because fit range
cannot move σ(150) — a structural flaw), nominal σ(150) row (25.3/28.3/39.1/41.9), total-syst RMS,
and a floor block (b 19.5/21.6/34.6/19.8 with syst/stat).

## 2. Which entries are pre-fix
ALL numerical entries. `paperSystematics.C` includes the production `tebSigma` (so a rerun would
pick up the post-fix width extraction) BUT its in-memory mirror builds (DW−UP)/2 WITHOUT the
in-event channel-consistency veto — i.e. a rerun would still not be the production chain. The
nominal values (25.3 etc.) and floors are the retired pre-fix set.

## 3. What is recomputed now (this script, full production chain)
Per build at 150 GeV, one pass storing per-event raw end times/peaks for the ADOPTED source, then
post-hoc variations on identical stored events: K=500/2000 (vs 1000), r_fid=2.5/3.5 (vs production
3.0 at 150 GeV), MCP upper 700 (vs 750), HG threshold 30 (vs 20 mV), **veto window 1.5/3.0 ns (vs
2.0 — a NEW post-fix knob that must be in the budget)**. Width = production post-fix `tebSigma`.
Total = RMS of shifts. Plus the DSB1 floor block recomputed from a fresh production
`timingBrightestK` 6-energy scan: b ± stat (√χ² inflated) and the fit-range variation (drop
25 GeV → Δb) — placed in the FLOOR block where it belongs, not the σ(150) block.

## 4. Methodological caveats (NOT numerical systematics — moved to table notes)
- Timing-method choice (srCFD vs cfd05/LED): treated in §5.3 with its own paired CI; not folded in.
- MIXED module-wide ill-posedness: a methodological caveat, not a variation.
- TENERGY √(4/3) capillary-count penalty: a known statistical factor, stated, not varied.
- MIXED same-shower ratio uncertainty (±0.05 energy-period scatter) and run-jackknife stability
  (<0.01): GATE-6 recomputed numbers, cited in notes.
- Depth-dial acceptance dependence: belongs to §depth scoping, not this selection budget.

## 5. Removals / softenings
- The σ(150) "fit 50–150 only" row: REMOVED (structurally zero); replaced by a floor-block
  fit-range entry for DSB1.
- Floor systematics for LUAG/MIXED/TENERGY: NOT re-quoted numerically (each would need a full
  6-energy scan per variation — not run); replaced by the honest qualified-floor sentence
  (19–26 ps span) already in prose + the DSB1-only floor block. Better bounded honesty than
  fake precision (per instruction).

## Outputs
`systematicsPostfix.C` (this dir) → regenerated `papers/timing/tab_systematics.tex` (only
defensible post-fix entries + notes), log `systematics_postfix_result.txt` (this dir),
md mirror `papers/tables/systematics_postfix_2026-06-09.md`.
